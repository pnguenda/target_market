            # save to sqlite database
			# this code belongs in app.py. I cannot get it to run at the moment

            blob = sqlite3.Binary(plt.imread(new_image))

            blob = str(base64.b64encode(blob)

            print(blob)

            conn = sqlite3.connect('database/database.sqlite')
            
            c = conn.cursor()

            c.execute(f"INSERT INTO images (datetime, address, image) VALUES ('test1', 'test2', {blob});")

            conn.commit()

            conn.close()
